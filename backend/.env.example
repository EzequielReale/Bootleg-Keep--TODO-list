# Server
PORT=3000

# Database (Option 1: Individual values for local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=notes_app

# Database (Option 2: Connection URI, used in production like Render)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/notes_app

# Environment
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173
